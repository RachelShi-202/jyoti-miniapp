const release=require('../../release-config');Page({data:{sourceUrl:release.sourceUrl||'',version:release.version},copySource(){if(this.data.sourceUrl)wx.setClipboardData({data:this.data.sourceUrl})}})
